apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-service
  namespace: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: front-service
  template:
    metadata:
      labels:
        app: frontend-service
    spec:
      containers:
      - name: frontend
        image: 858507113889.dkr.ecr.ap-northeast-2.amazonaws.com/argocd/front-service:40cdc1321be518a44c612122ba37b8104e6eb942
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: front-service
  namespace: argocd
  annotations:
    # AWS Load Balancer Controller를 사용하는 경우 (필요 시 수정)
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector:
    app: front-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80