{
  "name": "ctrlf-front",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:chatbot": "npm -w apps/chatbot run dev",
    "dev:groupware": "npm -w apps/groupware run dev",
    "build:chatbot": "npm -w apps/chatbot run build",
    "build:groupware": "npm -w apps/groupware run build",
    "preview:chatbot": "npm -w apps/chatbot run preview",
    "preview:groupware": "npm -w apps/groupware run preview",
    "lint": "npm -ws run lint"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3"
  }
}
